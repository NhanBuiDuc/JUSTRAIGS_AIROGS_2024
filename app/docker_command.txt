docker build --tag app .; `
    Remove-Item -Path test\output\* -Force; `
    docker run --rm --gpus all `
    --volume ${PWD}\test\input:/input `
    --volume ${PWD}\test\output:/output `
    app
docker save app | gzip -c > app.tar.gz

$ cd ./app
$ docker build --tag app . && \
    rm -f test/output/* && \
    docker run --rm --gpus all \
    --volume $(pwd)/test/input:/input \
    --volume $(pwd)/test/output:/output \
    app

docker save app | gzip -c > app.tar.gz